import { SxProps } from "@mui/material";

const experiencesContainer: SxProps = {
  display: "grid",
  gridTemplateColumns: "repeat(auto-fill, 350px)",
  columnGap: "100px",
  rowGap: "50px",
  mt: 3,
  justifyContent: "center",
  alignItems: "center",
};

const styles = { experiencesContainer };

export default styles;
